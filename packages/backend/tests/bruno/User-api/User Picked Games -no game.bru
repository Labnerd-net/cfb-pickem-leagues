meta {
  name: User Picked Games -no game
  type: http
  seq: 9
}

post {
  url: http://{{host}}/api/userPicks
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "year": "2025",
    "week": "1",
    "seasonType": "regular",
    "games": [
      {
        "game": 100,
        "pick": "home_team"
      }
    ]
  }
}

assert {
  res.status: eq 404
  res.body.message: eq Not Found
}

settings {
  encodeUrl: true
  timeout: 0
}
