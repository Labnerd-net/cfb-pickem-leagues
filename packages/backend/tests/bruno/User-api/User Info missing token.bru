meta {
  name: User Info missing token
  type: http
  seq: 4
}

get {
  url: http://{{host}}/api/user/profile
  body: none
  auth: inherit
}

body:json {
  {
    "email": "brian@email.com",
    "password": "monkey"
  }
}

assert {
  res.status: eq 401
  res.body: eq Unauthorized
}

settings {
  encodeUrl: true
  timeout: 0
}
