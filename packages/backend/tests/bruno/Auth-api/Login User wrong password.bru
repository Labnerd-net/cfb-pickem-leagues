meta {
  name: Login User wrong password
  type: http
  seq: 8
}

post {
  url: http://{{host}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "brian@email.com",
    "password": "monkey2"
  }
}

assert {
  res.body.error: eq "Invalid credentials"
  res.status: eq 401
}

settings {
  encodeUrl: true
  timeout: 0
}
