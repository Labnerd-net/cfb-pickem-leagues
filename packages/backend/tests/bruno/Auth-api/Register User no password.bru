meta {
  name: Register User no password
  type: http
  seq: 5
}

post {
  url: http://{{host}}/api/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "email": "brian@email.com"
  }
}

assert {
  res.body.error: eq 'Email and password required'
  res.status: eq 400
}

settings {
  encodeUrl: true
  timeout: 0
}
