meta {
  name: Login Admin
  type: http
  seq: 2
}

post {
  url: http://{{host}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@email.com",
    "password": "monkey2"
  }
}

assert {
  res.body.data.token: isString
  res.status: eq 200
}

script:post-response {
  bru.setVar("token", res.body.data.token);
  bru.setVar("id", res.body.data.id);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
