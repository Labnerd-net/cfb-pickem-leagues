meta {
  name: Register Admin
  type: http
  seq: 1
}

post {
  url: http://{{host}}/api/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@email.com",
    "password": "monkey2"
  }
}

assert {
  res.body.data.token: isString
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
