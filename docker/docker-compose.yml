services:
  cfb-backend:
    image: ghcr.io/labnerd-net/cfb-pickem/backend:latest
    container_name: cfb-backend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      DB_HOST: cfb-db
      DB_PORT: 5432
      DB_NAME: cfb-pickem
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_SSL: false
      JWT_SECRET: ${JWT_SECRET}
      CLIENT_URL: ${CLIENT_URL}
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      NOTIFICATION_FROM_EMAIL: ${NOTIFICATION_FROM_EMAIL:-}
    depends_on:
      cfb-db:
        condition: service_healthy

  cfb-frontend:
    image: ghcr.io/labnerd-net/cfb-pickem/frontend:latest
    container_name: cfb-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      BACKEND_URL: ${BACKEND_URL:-http://cfb-backend:3000}

  cfb-db:
    image: postgres:16-alpine
    container_name: cfb-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: cfb-pickem
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
