# Current Feature

## Current Feature Spec File

None

## Current Feature Plan File

None

## History

<!-- Keep this updated. Earliest to latest -->
- **Auth and Input Validation Security Fixes** (backlog #1, #4, #5, #6): Added `game.picked` enforcement on `POST /user/picks`; applied `zValidator` to `POST /auth/register` and `POST /auth/login`; raised password min to 8 chars and enforced max 72 (bcrypt truncation limit); fixed middleware ordering on 6 routes so `authMiddleware` runs before validators; updated frontend registration schema to match.
- **Picks Transaction Rollback and Weeks Unique Constraint** (backlog #9, #12): Wrapped `POST /user/picks` inserts in `db.transaction()` via new `addPickedGamesBatch` — partial pick commits on mid-batch failure are no longer possible. Confirmed backlog [12] false positive: `admin.weeks(year, week_number)` primary key already enforces uniqueness.
- **Cron Week Reset, Settings Error Handling, Email Transporter Singleton** (backlog #10, #11, #17): Added `lastWeekKey` tracking to `cronTick.ts` so `hardCapStart` and `lastRefreshAt` reset when the active week changes. Wrapped Settings `useEffect` in `try/finally` with `loadError` state to prevent infinite spinner on network failure. Moved `nodemailer.createTransport` to module scope in `emailSender.ts` as a conditional singleton.
- **Email XSS and Rate Limiter IP Spoofing Fix** (backlog #2, #3): Added `escapeHtml()` helper in `templates.ts` and applied it to `displayName` in `rankingsUpdatedTemplate` to prevent stored XSS in leaderboard emails. Added `TRUST_PROXY` env var (default `false`) to `envVars.ts`; rate limiter now uses socket remote address when `TRUST_PROXY=false`, preventing IP spoofing on auth endpoints. Unit tests added for both fixes.
- **DB Connection Options and Admin Bootstrap Security Fix** (backlog #7, #8): Replaced template-literal Postgres URL with structured `pg.Pool` config object in `db/index.ts`, wiring in `DB_SSL` support — passwords with special characters no longer malform the connection string. Added `returnTotalUserCount()` to `dbUserFunctions.ts` that sums active + deleted users; `POST /auth/register` now uses this count for the admin bootstrap check, preventing privilege re-escalation if the sole admin deletes their account. Unit and route tests added for both fixes.
- **Error Boundary Hookup and Dead Code Removal** (backlog #13, #14): Wired `<ErrorBoundary>` into `App.tsx` wrapping `<BrowserRouter>` so unhandled render errors show a fallback UI instead of a blank screen. Removed dead `addGameToWeek` function from `dbAdminFunctions.ts` — all callers already used `upsertGameForWeek`.
- **Picks N+1 Query and Start Time Index Performance Fix** (backlog #15, #19): Replaced per-pick `returnGame` loop in `POST /user/picks` with a single `returnGamesBulk` bulk fetch using `inArray`; removed redundant `returnGame` call inside `addPickedGame`. Added `index('games_start_time_idx')` on `admin.games.startTime` and applied migration. Fixed pre-existing malformed `0002_snapshot.json` index column format blocking `drizzle-kit generate`.
- **Notification Bulk Query and Rate Limiter Interval Cleanup** (backlog #16, #18): Replaced per-user `hasNotificationBeenSent` loop in `dispatcher.ts` with a single `returnSentNotificationUserIds` bulk query returning a `Set<number>` — N email-channel queries reduced to 1. Stored the `setInterval` handle in `rateLimiter.ts` and extended `clearRateLimitStore()` to call `clearInterval`, preventing interval leaks in test suites and enabling graceful shutdown. Tests added for both changes.
- **Week Param Validation Refactor** (backlog #22, #26, #32): Added `yearQuerySchema` and `weekIdentifierQuerySchema` (using `z.coerce.number()`) to `zValidate.ts` and exported `yearQueryValidator` / `weekIdentifierQueryValidator`. Replaced all manual `isNaN`/range-check blocks across `user.ts`, `leaderboard.ts`, and `admin.ts` with these validators. Renamed `?week=` query param to `?weekNumber=` on all backend routes; updated frontend callers in `userRequests.ts` and `adminRequests.ts`; updated affected tests.
- **Fix Hono RPC Type Casts in userRequests**: Removed all `as unknown as` casts from `userRequests.ts`. Added `InferResponseType` aliases (`UserPicksRPC`, `AdminGamesRPC`) to handle `Date → string` serialization over JSON; exported `UserPickWire` and `AdminGameWire` for downstream components. Fixed `GetWeeksResponse` to use `AdminWeekData[]` (not `AdminDbWeekData[]`). Added `extractError()` helper consolidating 6 repeated error-path casts into one. Moved `BroadcastChannelInfo` to shared types. Updated `WeekGameSection.tsx` and `UserPicksGameCard.tsx` to use wire types with explicit `new Date()` conversion at usage sites.
- **Admin and User Section Component Refactor** (backlog #23, #24): Extracted `useWeekManagement` and `useGameManagement` hooks from `AdminSection.tsx` (369 → 128 lines); extracted `useWeekGames` hook from `WeekGameSection.tsx` (305 → 90 lines) and split the dual render path into `WeekPicksView` and `WeekResultsView` sub-components. The `cancelled` flag stale-request pattern in the games-and-picks effect was preserved and extended to the year-change effect. No behavior changes.
- **Pick Voided Status on Game Deletion**: Added `'voided'` to the `Team` type in shared types. Changed `user_games_admin_games_fk` FK action from `onDelete('cascade')` to `onDelete('restrict')` to prevent silent pick destruction on game deletion. Filtered voided picks from all 5 scoring/count DB functions (`returnUserGames`, `returnUserPickCount`, `returnWeekScores`, `returnUserPickHistory`, `returnLeaderboard`) including the `pending` CASE WHEN in the leaderboard LEFT JOIN. Generated Drizzle migration. Added unit tests for all voided exclusion paths.
- **Remove SportsDataverse Data Source**: Deleted `src/api/sdv.ts` and `src/types/sportsdataverse.d.ts`; removed `sportsdataverse` npm package (29 packages pruned). Added fail-fast startup validation in `envVars.ts` — any `DATA_SOURCE` value other than `ncaa` or `cfbd` now throws a fatal error. `sdv` was already dead code (no branch in `api/index.ts` converters).
- **Backlog Fixes: Timezone, Tab Indices, Env Validation, Log Pagination** (backlog #28, #30, #31, #33): Added `timeZone: 'America/Chicago'` to `picksReminderTemplate` kickoff time formatting — emails no longer show server-local timezone. Replaced the `currentTab === N` ternary chain in `Dashboard.tsx` with a tab-descriptor array. Refactored `envVars.ts` to parse all env vars through a single Zod schema with `.superRefine()` for cross-field validation (`CFBD_API_KEY` required only when `DATA_SOURCE=cfbd`); exported `validateEnv()` helper for tests. Added `offset` param to `returnNotificationLogs` and `GET /admin/notification-logs`; `NotificationLogSection` now paginates with Previous/Next buttons and shows current page and total count. Unit and route tests added for all three backend changes.
- **Notification Log Server-Side Filtering** (backlog #53): Moved channel and notification-type filtering from client-side to server-side. Added optional `channel` and `notificationType` query params to `GET /admin/notification-logs` (Zod enum validation, 400 on invalid values) and to `returnNotificationLogs` (conditional Drizzle `WHERE` clause applied to both rows and count queries). `NotificationLogSection` now passes filter state to the API call and drops the client-side `filtered` array; `total` count and pagination now reflect the active filter. Unit and route tests added for all filter combinations.
- **CFBD-Only Data Source with Spread Support**: Deleted `ncaa-api.ts` and removed all NCAA adapter branches from `api/index.ts`. Removed `DATA_SOURCE` env var; `CFBD_API_KEY` is now unconditionally required at startup (Zod `min(1)`). Removed `ncaaGameId` column from `admin.games` schema and all shared types/DB functions; added nullable `spread` (`real`) column. Lines data is fetched in parallel with game data via `Promise.all` using CFBD's `getLines` endpoint — consensus line preferred, falls back to `lines[0]`, null if no lines. Spread displayed on admin `GameCard` when non-null; user-facing views unchanged. Drizzle migration generated. `DataSource` type removed from shared types. Unit tests added for all spread mapping paths and updated envVars tests.
- **User Profile Update**: Added `PATCH /user/profile` endpoint accepting optional `displayName` and/or `currentPassword`+`newPassword`. Empty body returns 400; wrong current password returns 401; `newPassword` validated via existing `validatePassword()`. On success the JWT cookie is re-issued so `AuthProvider` picks up the new display name immediately. Added `updateUserProfile()` DB function and `updateProfileValidator` Zod schema. Frontend Settings page gains a Profile section above Account: an editable display name field (pre-filled from auth context, saves and refreshes the JWT) and a change-password form. Route and schema tests added.
- **Delete Year Data**: Added `DELETE /admin/year/:year` endpoint that blocks deletion with 409 if any user picks exist for the year, otherwise deletes all `admin.games` then `admin.weeks` in FK-safe order (games first). Added `hasPicksForYear`, `deleteGamesForYear`, `deleteWeeksForYear` DB functions. Frontend gains a "Reset Year" button (visible whenever weeks are loaded) with a confirmation dialog; on 409 shows the blocking message, on success clears week/game state. Route tests cover 401, 403, 400 (invalid/out-of-range year), 200 (no-op), 200 (cleanup), and 409 (picks exist).
- **Admin Score Correction**: Added `PATCH /admin/games/:gameId/score` (production-safe, admin-only) that recalculates `winningTeam`, writes an audit row to the new `admin.score_corrections` table, and fires `rankings_updated` if all picked games for the week become complete. Added `correctGameScore()` DB function wrapping update + audit insert in a transaction. Added `correctGameScoreParamValidator` and `correctGameScoreBodyValidator` (non-negative integers). Admin `GameCard` shows an edit icon next to "PICKED" badge on picked games only; clicking opens a dialog pre-filled with current scores that submits the correction and updates the game card inline. Migration generated. Route and DB function tests added.
- **Admin User Tools** (CSV Export + Broadcast Notification): Added `GET /admin/users/export` returning merged user + all-time pick totals (`returnUserPickTotals` DB function, LEFT JOIN so zero-pick users included). Frontend "Export CSV" button downloads `users-export-YYYY-MM-DD.csv` with Display Name, Email, Roles, Total Picks, Correct Picks, Accuracy columns; button disabled while loading or export in-flight. Added `POST /admin/notifications/broadcast` (Zod-validated subject/message/overrideEmailPreferences) that calls `dispatchAdminBroadcast` — free-form email (escaped HTML body) + ntfy/Telegram/Discord broadcast channels, no deduplication. `overrideEmailPreferences=true` sends to all emailVerified users, false uses opt-in list. `resolveWeekContext` DB helper resolves active week for log context; falls back to upcoming-week year (mid-season gap) or `maxYear+1` (off-season); `weekNumber=0` displayed as "Pre-season" in notification log. `admin_broadcast` added to `NotificationType`. Route and DB unit tests added for both endpoints.
- **24hr Picks Reminder and Notification Type Rename** (backlog #41 partial): Added `picks_reminder_24h` notification type firing in the 25h–24h window before first kickoff, mirroring the existing 1hr reminder pattern. Added `shouldSend24hrReminder` to `cronLogic.ts`, `reminder24hSentForWeek` state flag to `cronTick.ts`, `picksReminder24hTemplate` to `templates.ts`, and wired through dispatcher, zValidate, admin route filter, and frontend Settings/NotificationLogSection. Renamed `picks_reminder` → `picks_reminder_1h` across all layers (shared types, backend, frontend, tests) for consistency.
- **Env Var Direct Access Fixes** (backlog #3, #4): Replaced `process.env.CLIENT_URL?.split(',')[0]` in `auth.ts` (both `POST /register` and `POST /resend-verification`) with `clientURLs[0]` imported from `envVars.ts`. Moved `process.env.TRUST_PROXY` read in `rateLimiter.ts` from per-request to module-level import of `trustProxy` from `envVars.ts`. Updated `rateLimiter.test.ts` to use `vi.resetModules()` + `vi.doMock` for the `TRUST_PROXY=true` test block; added `trustProxy: false` to envVars mocks in `picks-deadline.test.ts` and `devEndpoints.test.ts`.
- **TypeScript Type Safety Fixes** (backlog #11, #12): Made `AdminGameData.gameId` optional and overrode it as required in `AdminDbGameData` — reflects that `gameId` is DB-assigned (absent during construction, always present after DB read). Replaced both `{} as AdminWeekData` / `{} as AdminGameData` empty-cast mutation patterns in `api/index.ts` with typed object literals. Replaced all ~20 `as unknown as` double-casts in `adminRequests.ts` with `InferResponseType<>` aliases for 8 admin endpoints, an `extractError` helper for all 11 error paths, and exported `AdminDbGameDataWire` / `AdminDbWeekDataWire` wire types. Updated 6 downstream admin components and hooks to use wire types.
- **Leaderboard Week View** (backlog #20): Added Season/Week tab toggle to `LeaderboardSection.tsx`. Season view is unchanged. Week view adds year + week selectors (populated via `getWeeksForYear`), defaults to the most recent week, and shows per-week scores from `GET /leaderboard/scores` with columns: Correct, Incorrect, Pending, Total, % (derived). Rows sorted by correct desc, incorrect asc. Stale-request cancelled flag pattern applied to both data-loading effects. Added `getWeekScores` to `leaderboardRequests.ts`; MSW handler, API tests, and component tests added.
- **Cron State and Spread Field Documentation** (backlog #13, #17): Expanded the bare module-level comment in `cronTick.ts` to document that in-memory cron state resets on restart by design — the `hasNotificationBeenSent` DB check in `dispatcher.ts` is the user-facing safety net against double-sends; only observable side effect is an extra CFBD API refresh. Added inline comment on `spread` in `admin.ts` schema documenting it is populated from CFBD lines and reserved for a future "against the spread" pick mode, not used in current scoring or display.
- **Email Validation Dead Code and ErrorBoundary Logger** (backlog #7, #18): Removed unreachable `!email || typeof email !== 'string'` guard from `validateEmail()` — the `string` parameter type and upstream Zod validation make it dead code; updated the empty-string test to expect `'Invalid email format'`. Added a comment to `ErrorBoundary.componentDidCatch` documenting why `console.error` is used directly instead of the logger abstraction (error boundary crashes must always be visible regardless of `VITE_LOG_LEVEL`).
- **UsersSection and useWeekGames Refactor** (backlog #14, #15): Extracted CSV blob/download into `src/lib/exportCsv.ts` (`downloadCsv` utility). Extracted broadcast dialog into `BroadcastDialog.tsx` (self-contained, manages own form state). `UsersSection.tsx` reduced 295 → 195 lines. Split `useWeekGames.ts` into `useWeekNavigation` (initialization, year/week effects, games fetch) and `usePickSubmit` (picks fetch, submission, snackbar); `useWeekGames` becomes a thin composition hook (240 → 74 lines). `cancelled` flag stale-request pattern preserved in all three async effects. `WeekGameSection.tsx` required no changes.
- **Game Card Countdown Timer and Lock Warning** (backlog #21): Added a countdown label to each unlocked `UserPicksGameCard` showing "Locks in X h Y m" (or minutes/seconds as the game approaches). Label and card border turn red when under 1 hour. Countdown is driven by a shared `useCountdownTick` hook with an adaptive interval (5s normally, 1s within 2 minutes). Added a `LockWarningDialog` that appears when the first game is within 15 minutes of locking and the user has unsaved picks; dialog offers Submit Now or Dismiss (dismissed-once per week/session). All new behaviour gated behind `VITE_IGNORE_PICK_DEADLINE`. New files: `countdownFormat.ts`, `useCountdownTick.ts`, `LockWarningDialog.tsx`. Tests added for all new units and the dialog trigger logic.
- **Admin Password Reset**: Added `PATCH /api/admin/users/:id/password` (admin-only) that validates the new password via `validatePassword()`, hashes with bcrypt, and updates the user's `passwordHash`. Blocks self-reset with 403. Added `updateUserPassword()` DB function, `resetPasswordParamValidator` + `resetPasswordBodyValidator` Zod validators. Frontend: new `ResetPasswordDialog.tsx` (password + confirm fields, submit disabled until matching and 8–72 chars); "Reset Password" button per user row in `UsersSection.tsx` (hidden for current user); success alert shown after reset. 7 backend route tests added.
- **Multi-League Phase 1 — Schema & Migration**: Added `leagues`, `league_members`, and `league_games` tables to the `public` schema. Added `'public'` to `drizzle.config.ts` `schemaFilter`. Migration seeds a Default League from the first admin user, populates all existing users into `league_members` with correct roles, and populates `league_games` from currently `picked = true` games. No existing tables, routes, or frontend code changed. Build passes; all 461 tests pass.
- **Multi-League Phase 2 — League API**: Added `src/routes/leagues.ts` mounted at `/api/leagues` with 8 endpoints: create league (site admin), list user's leagues, join via invite code, get league detail, list members, change member role, remove member, and regenerate invite code. Added `src/db/dbLeagueFunctions.ts` with 12 DB functions. Added `requireLeagueMembership(role?)` middleware to `middleware.ts` (404 on unknown league, 403 on non-member, sets `leagueMembership` on context). League admin guards block last-admin demotion/removal (409). Invite code hidden from non-admin members. Added `LeagueRole`, `LeagueData`, `LeagueMemberData` to shared types. League validators added to `zValidate.ts`. Test schema updated with league tables. 22 route tests added; all 325 tests pass. Vitest `maxWorkers: 4` added to cap PGlite concurrency.
- **Multi-League Phase 3 — League-Scoped Game Selection, Picks & Leaderboard**: Removed `picked` column and indexes from `admin.games`; added `league_id` to `user.games` with PK changed to `(userId, gameId, leagueId)`. Replaced `setPickedGames`/`returnPickedGames` with four new DB functions: `getGamesForLeagueWeek`, `getGlobalGamesWithLeagueStatus`, `addGameToLeague`, `removeGameFromLeague`. Added `src/routes/adminLeagues.ts` mounted at `/api/admin/leagues` with 5 endpoints for league game pool management (GET annotated cache, POST/DELETE game, POST complete, PATCH score). Scoped `GET /user/picks`, `GET /user/games`, `GET /user/history`, `GET /leaderboard`, `GET /leaderboard/scores`, and `POST /user/picks` to require `leagueId` + league membership. `returnLeaderboard` and `returnWeekScores` filter by league membership and scoped picks. Removed `POST /admin/picks`; `rankings_updated` dispatch deferred to Phase 6. Frontend API shims use hardcoded `leagueId: 1` as a Phase 4 bridge. Drizzle migration generated. 23 new route tests added; all 346 tests pass.
- **Multi-League Phase 4 — Frontend League Switcher & Join Flow**: Added `LeagueContext` (`src/contexts/LeagueContext.tsx`) providing `leagues`, `activeLeague`, `setActiveLeague`, `isLoading`, and `refetchLeagues`; fetches after auth resolves, restores active selection from `localStorage`, clears on logout. `LeagueProvider` wired between `AuthProvider` and `ErrorBoundary` in `App.tsx`. Added `LeagueSwitcher` component to the Navbar — static label for 1 league, MUI `Select` dropdown for 2+. `Dashboard.tsx` gates on `activeLeague` — shows a spinner while loading and `JoinLeaguePrompt` when user has no leagues. `JoinLeaguePrompt` (full-page card) and `JoinLeagueDialog` (modal) both handle 404/409/generic errors inline and call `refetchLeagues` on success. Settings page gains a Leagues section listing memberships and a "Join another league" button. Added `leagueRequests.ts` (`getLeagues`, `joinLeague`) using the Hono RPC client. Replaced all 5 hardcoded `leagueId: '1'` shims in `userRequests.ts` and `leaderboardRequests.ts` with a `leagueId: number` parameter; `usePickSubmit`, `useWeekNavigation`, and `LeaderboardSection` read `activeLeague?.leagueId` from `useLeague()` and pass it through. MSW handlers, API tests, and component tests added; all 181 frontend tests pass.
- **Multi-League Phase 5 — League-Scoped Frontend Screens**: Completed the multi-league frontend. Picks, games, and leaderboard were already scoped to `activeLeague.leagueId` from Phase 4; Phase 5 adds the admin-side league management UI. Extracted score correction dialog into reusable `ScoreCorrectionDialog.tsx`. Added `LeagueAdminSection.tsx` (new Dashboard tab gated on `activeLeague.role === 'admin'`) with game pool checkboxes (add/remove via `POST`/`DELETE /api/admin/leagues/:id/games/:gameId`), optimistic UI with 409 revert for games with picks, and "Mark Week Complete" button. Added `LeagueSettingsSection.tsx` (invite code display/copy/regenerate with confirmation, member table with promote/demote/remove — 409 propagated to inline error). Added `CreateLeagueDialog.tsx` (site admin only, two-step: form → invite code reveal). Settings page gains a gear icon per admin-role league opening `LeagueSettingsSection` in a dialog, and a "Create league" button for site admins. `Dashboard.tsx` tab list rebuilt as a conditional array to avoid index drift. Added 5 league game management functions to `adminRequests.ts` and 5 member/create functions to `leagueRequests.ts` with `InferResponseType` wire types. MSW handlers and API tests added for all 10 new functions; all 210 frontend + 346 backend tests pass.
- **Multi-League Phase 6 — League-Scoped Notifications**: Made all game-complete and picks-reminder notifications league-aware. Added `league_id` column to `notification_log` with unique constraint `(user_id, league_id, year, week_number, notification_type, channel)` — a user in two leagues now receives one notification per league without either being deduped. `returnEmailOptedInUsers` filters by `leagueMembers` when `leagueId > 0`; site-wide sentinel `leagueId = 0` covers admin broadcast. `hasNotificationBeenSent`, `addNotificationLog`, and `returnSentNotificationUserIds` all take `leagueId`. New `getActiveLeaguesForWeek` returns leagues with games for a given week; new `getLeaguesForGame` returns leagues containing a specific game. `dispatchNotification` accepts `leagueId`/`leagueName`; `dispatchGameComplete` wrapper looks up the league name and dispatches `rankings_updated` scoped to that league. All three email templates include `[LeagueName]` in the subject. Cron loop iterates over active leagues per-week using per-league `scoresCompletedForLeague`, `reminder24hSentForLeague`, and `reminder1hSentForLeague` Sets (all reset on week change). `adminLeagues.ts` mark-complete re-fetches game state after completion and guards dispatch on `isWeekComplete`; score correction uses `updated.year`/`updated.weekNumber` from the DB (not client query params) for the completion check. Admin broadcast remains site-wide. Migration `0009_young_meltdown.sql` generated. 369 backend + 210 frontend tests pass.
- **Migrate Backend to Cloudflare Workers**: Swapped `node-postgres` for `@neondatabase/serverless` HTTP driver; replaced `bcryptjs` with Web Crypto PBKDF2 via `crypto.subtle` (100k iterations — Workers runtime cap); added Cloudflare KV-backed rate limiter with in-memory fallback; split `src/index.ts` into `server.ts` (Node.js entry) and `worker.ts` (Workers entry with `fetch`+`scheduled` export); refactored `createLeague` and `correctGameScore` to remove interactive transactions (HTTP driver limitation); added `wrangler.jsonc` at repo root with `nodejs_compat`+`nodejs_compat_populate_process_env` flags, KV namespace binding, and static asset config serving the Vite frontend from the same Worker with SPA routing; moved `setInterval` cleanup to lazy init to avoid Workers global-scope restriction; auto-create Default League on first admin registration to avoid bootstrap deadlock; added `PATCH /api/leagues/:leagueId` rename endpoint with inline edit UI in League Settings. All 369 backend tests pass.
- **Admin Broadcast Notifications — League-Scoped and Platform-Wide**: Split single site-wide admin broadcast into two distinct types. Added `league_channels` table (per-league ntfy/Telegram/Discord config); `getLeagueChannels`, `upsertLeagueChannels`, `getLeagueMembersWithEmail` DB functions. Refactored all three broadcast senders (`ntfySender`, `telegramSender`, `discordSender`) to accept config as params instead of reading env vars. `dispatchAdminBroadcast` is now email-only (platform admin); `dispatchNotification` fetches league channels from DB; new `dispatchLeagueBroadcast` sends email + league-configured channels. Added `GET/PATCH /api/leagues/:leagueId/channels` (league admin) and `POST /api/leagues/:leagueId/broadcast` (league admin). `GET /user/notifications/channels` is now league-aware via `?leagueId=`. Removed broadcast channel env vars (`NTFY_TOPIC_URL`, `TELEGRAM_*`, `DISCORD_*`) from `envVars.ts`. Added `waitUntil` utility for safe Workers/Node.js compatibility. Frontend: channel config form + Send Message to League form added to `LeagueSettingsSection`; Settings page channels load per active league; `BroadcastDialog` updated to indicate email-only. Migration `0010_lean_next_avengers.sql` generated. New test files: `leagueBroadcast.test.ts`, `leagueChannels.test.ts`; updated `adminBroadcast.test.ts` and `senders.test.ts`. 384 backend + 210 frontend tests pass.
- **League Invite Link with Confirmation Screen**: Added `GET /leagues/invite/:inviteCode` public endpoint returning `{ leagueId, leagueName }` (no auth required, 404 for invalid codes). Added `/join/:inviteCode` frontend route with a standalone full-page `JoinLeagueConfirm` page (no navbar). Confirmation page handles: loading, invalid code error, unauthenticated state (login/register buttons with redirect param preserved), already-a-member detection on load (via `leagues` list from LeagueContext), and join flow (calls `POST /leagues/join`, sets active league, navigates to dashboard). Updated `LoginForm` and `RegistrationForm` to support `?redirect=` query param — cross-links between login/register also carry the param. Added "Copy link" button to `LeagueSettingsSection` that copies `origin/join/:inviteCode` to clipboard.
- **Score Correction Restricted to Platform Admin; Sync Results from CFBD**: Removed score correction endpoint from league admin routes (`adminLeagues.ts`) and hid the edit button in `LeagueAdminSection` for non-platform-admins. Replaced manual "mark game complete" flow with a platform-admin-only `POST /admin/weeks/sync-results` endpoint that re-fetches game data from CFBD, upserts completed scores, skips games with correction audit entries (`getCorrectedCfbdGameIds`), and dispatches `rankings_updated` per league when their full pool is complete. Removed `POST /admin/games/complete`, `POST /admin/leagues/:leagueId/games/complete`, `markGameComplete` DB function, `markGameCompleteValidator`, `MarkCompleteCard` frontend component, and "Mark Week Complete" button from `LeagueAdminSection`. Added "Sync Results" button to `AdminSection` with loading state and summary snackbar (`gamesCompleted`, `gamesSkipped`, `leaguesNotified`). 6 sync-results route tests added; stale mark-complete and league score correction tests removed. 376 backend tests pass.
- **Workstream A Security Fixes** (backlog #27, #29, #30, #32): Added league membership check to `GET /user/notifications/channels` when `leagueId` is present (non-members now get 403). Added `z.url()` validation (Zod v4 top-level API) to `ntfyTopicUrl`, `telegramInviteUrl`, `discordWebhookUrl`, and `discordInviteUrl` in `leagueChannelBodySchema` — prevents arbitrary/internal URLs from being stored (SSRF mitigation). Increased invite code entropy from `randomBytes(4)` to `randomBytes(8)` (32-bit → 64-bit). Added `.max(50)` to the `games` array in `allUserPickedRequestSchema` to cap per-request DB lookups.
- **Workstream B Security Fixes** (backlog #26, #28): Added 24-hour expiry to `markEmailVerified` — tokens older than 24h now return null and the route returns 400. Added orphan check to `DELETE /auth/deleteUser` — returns 409 if the user is the sole admin of any league, with the league name in the message. Also fixed `deleteUserWithAudit` to delete league memberships within the transaction before deleting the user (pre-existing FK constraint gap). Tests added for expired token (DB unit + route), and for deleteUser (401 unauthed, 409 sole admin, 200 non-admin).
